"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-email-heartbeat"
description = "Allows the email webview to send heartbeat pings to the watchdog."
commands.allow = ["email_heartbeat"]
