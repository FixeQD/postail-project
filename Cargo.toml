[workspace]
members = [
  "src-tauri",
  "crates/html_transpiler",
  "crates/email_network",
  "crates/email_webview",
  "crates/postail_security",
]
resolver = "2"

default-members = ["src-tauri"]

[profile.release]
lto = "fat"
codegen-units = 1
strip = true
opt-level = "s"
panic = "abort"
