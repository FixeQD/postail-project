[workspace]
members = [
  "src-tauri",
  "crates/html_transpiler",
]
resolver = "2"

default-members = ["src-tauri"]

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
opt-level = "s"
panic = "abort"
